# Finance Dashboard — Working Guide

See [AGENTS.md](AGENTS.md) for the full working guide: architecture rules, model usage,
where things live, the import pipeline, and the `db.json` shape. It's the shared source of
truth for any coding agent (Claude Code, Cursor, Codex) working in this repo — keep it
updated instead of duplicating its content here.
